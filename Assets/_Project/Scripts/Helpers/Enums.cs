public enum GameState
{
    LOADING,
    INTRO,
    PLAY,
    PAUSE,
    GAME_OVER,
    VICTORY,
}

public enum Displays
{
    INTRO,
    BUILD,
    HUD,
    GAME_OVER,
    PAUSE,
    LANGUAGE,
}

public enum PickableObjects
{
    COIN,
}

public enum Languages
{
    PORTUGUES,
    ENGLISH,
}